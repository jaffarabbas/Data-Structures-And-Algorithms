#include <bits/stdc++.h> 
vector<vector<int>> findTriplets(vector<int>arr, int n, int K) {
	vector<vector<int>> ans;
	for (int i = 0; i < arr.size(); ++i)
	{
		for (int j = i+1; j < arr.size(); ++j)
		{
			for (int m = 0; m < arr.size(); ++m)
			{
				vector<int> t;
				if(arr[j]+arr[j]+arr[m] == k){
					
				}
			}
		}
	}
}

int main(){

}